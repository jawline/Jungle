function JungleSound() {
	
	
				if (!buzz.isSupported()) {
					alert("Your browser is too old, time to update!");
				}

				if (!buzz.isMP3Supported()) {
					alert("Your browser doesn't support MP3 Format.");
				}
	
	this.background = null;	
}

JungleSound.prototype.setBackground = function() {
	this.background = 
}
